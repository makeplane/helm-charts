
apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Release.Namespace }}
  name: {{ .Release.Name }}-space
  labels:
    app.name: {{ .Release.Namespace }}-{{ .Release.Name }}-space
    {{- include "plane.resourceLabels" $ | nindent 4 }}
  {{- with (include "plane.resourceAnnotations" $) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  type: ClusterIP
  {{- if not .Values.services.space.assign_cluster_ip }}
  clusterIP: None
  {{- end }}
  ports:
  - name: space-3000
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app.name: {{ .Release.Namespace }}-{{ .Release.Name }}-space

---

apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: {{ .Release.Namespace }}
  name: {{ .Release.Name }}-space-wl
  {{- include "plane.labelsAndAnnotations" (dict "context" $ "values" .Values.services.space) }}
spec:
  replicas: {{ .Values.services.space.replicas | default 1 }}
  selector:
    matchLabels:
      app.name: {{ .Release.Namespace }}-{{ .Release.Name }}-space
  template:
    metadata:
      namespace: {{ .Release.Namespace }}
      labels:
        app.name: {{ .Release.Namespace }}-{{ .Release.Name }}-space
        {{- include "plane.podLabels" (dict "context" $ "values" .Values.services.space) | nindent 8 }}
      annotations:
        timestamp: {{ now | quote }}
        {{- with (include "plane.podAnnotations" (dict "context" $ "values" .Values.services.space)) }}
        {{- . | nindent 8 }}
        {{- end }}
    spec:
      {{- include "plane.podScheduling" .Values.services.space }}
      {{- include "plane.podSecurityContext" . }}
      containers:
      - name: {{ .Release.Name }}-space
        {{- include "plane.containerSecurityContext" . }}
        imagePullPolicy: {{ .Values.services.space.pullPolicy | default "Always" }}
        image: {{ .Values.services.space.image | default "makeplane/space-commercial" }}:{{ .Values.planeVersion }}
        stdin: true
        tty: true
        resources:
          requests:
            memory: {{ .Values.services.space.memoryRequest | default "50Mi" | quote }}
            cpu: {{ .Values.services.space.cpuRequest | default "50m" | quote }}
          limits:
            memory: {{ .Values.services.space.memoryLimit  | default "1000Mi" | quote }}
            cpu: {{ .Values.services.space.cpuLimit | default "500m" | quote}}
        {{- if .Values.extraEnv }}
        env:
          {{- toYaml .Values.extraEnv | nindent 10 }}
        {{- end }}
      serviceAccount: {{ .Release.Name }}-srv-account
      serviceAccountName: {{ .Release.Name }}-srv-account
---